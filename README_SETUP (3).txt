VUPE MULTIVERSE-CORE SETUP

VupeCore MAX keeps Multiverse-Core as requested.

Purpose:
- world administration/import/teleport utilities
- VupeCore still owns gameplay warp routing
- PlotSquared owns the vupeplots plot world

Vupe gameplay worlds:
  spawn
  fishing
  pvp
  farm
  mine
  vupeplots (PlotSquared)

Recommended fresh setup:
1. Use PlotSquared /plot setup for vupeplots.
2. Run /vupe setup worlds for Vupe's other worlds.
3. VupeCore attempts to import the Vupe-owned worlds into Multiverse automatically.
4. Verify with /mv list.

Never create `vupeplots` as a normal Multiverse flat world first. Let
PlotSquared create it.
